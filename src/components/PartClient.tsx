'use client'

import { InterPartFront, InterUser, ShowMember } from "../../interface";

export default async function PartClient(part:InterPartFront,user:InterUser,pees:ShowMember[],petos:ShowMember[]){
    if(user.mode=='nong'){
        return<></>
    }
    

}